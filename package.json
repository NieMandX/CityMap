{
    "name": "citymap",
    "private": true,
    "version": "0.1.0",
    "description": "Single-viewport web road editor for spline-based 3D road generation.",
    "scripts": {
        "dev": "python3 -m http.server 4173 --bind 127.0.0.1",
        "ci:syntax": "node --experimental-default-type=module --check scripts/road-editor.js",
        "ci:verify": "npm run ci:syntax"
    }
}
